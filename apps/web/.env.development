# 开发期留空，走 Vite 代理（见 vite.config.js）
VITE_API_BASE_URL=
# 后端 API 地址（代理目标）；与 uvicorn 启动端口一致
# 若 8000 为旧进程，可先启动 8002：uvicorn app.main:app --host 127.0.0.1 --port 8002
VITE_API_PROXY_TARGET=http://127.0.0.1:8002
VITE_DEV_PORT=5173
