---
description: 开发前读PRD；列表/详情/新增编辑审批字段不得少；线框=业务；UI=现网；已开发须测完；提验前自测；上传须真实选文件
alwaysApply: true
---

# 原型对照 · 验收纪律（强制）

适用于本仓库功能开发、联调、测试与「提请用户验收」。

权威来源（改页/改流/改文案前必须打开对照）：

- PRD / 线框：`docs/01-PRD/`（商城 Phase1 → `21-内容获客商城-phase1/`，尤其 `01`/`02`/`06` HTML 锚点页）— **业务、流程、列表列、表单/详情/审批栏位、操作矩阵**
- 现网 UI 参照：已上线页面壳（`AppLayout` / `AdminLayout`）、Element Plus、同类 CRM 列表/详情/抽屉、`design-tokens.css`
- 体例与操作页规格：[PRD写作规范](docs/00-总览/PRD写作规范.md)（含 §2.5；规格表管栏位/校验/下拉，**不管视觉皮**）
- 测试：[测试用例标准](docs/05-测试与验收/测试用例/内容获客商城-phase1/) · `.cursor/rules/test-case-standards.mdc`
- 执行门禁：[内容获客平台-执行计划](docs/02-执行计划/内容获客平台-执行计划.md)

> 高保真包若存在仅作参考，**不再作为开发门禁**；以 PRD 栏位规格 + 现网皮为准。

## 0. 开发前必读 PRD（强制）

1. 动手改某页前，**必须先打开**对应 PRD HTML 锚点（例：P02 列表 → `#p02-list`；P03 审核 → `#p03-detail` / `#p03-approve`；A20 → `#a20-form`；P02-B → `#p02b-overview`），对照栏位、校验、显隐、Tab、操作矩阵。  
2. 说明/回复须写清对照锚点；未读 PRD 不得宣称「已按原型」。

## 0b. 列表页完备性（强制 · 禁止砍列砍筛）

凡 PRD 列表页，实现时至少具备：

| 项 | 要求 |
|----|------|
| **默认列** | 与 PRD「默认列」一致，**不得擅自减少**；可选列进「列设置」 |
| **高级筛选** | PRD 写明的筛选项须有入口（可默认收起） |
| **导出** | 工具栏有导出（按当前筛选） |
| **列设置** | 可显隐列；锁定列不可关 |
| **分页** | 须可选每页条数（10/20/50/100，默认 20） |
| **行操作** | 按 PRD 状态×操作矩阵显隐 |

## 0c. 详情 / 新增 / 编辑 / 审批页完备性（强制 · 禁止砍字段）

凡 PRD 中的 **详情页、新增/创建、编辑、审核/审批**（含抽屉/全页/子 Tab），实现时至少具备：

| 项 | 要求 |
|----|------|
| **栏位齐全** | PRD 规格表 / 线框中的展示字段、表单项（含必填/选填、按主体或状态显隐）**不得擅自减少** |
| **展示名原文** | 列表列名、表单 label、详情项、Tab、按钮文案须与 PRD **原文一致**，禁止自行改写成近义词（如「联系电话」不得改成「联系人手机」；「经营联系人」不得改成「联系人」） |
| **默认值与预填** | 按 PRD：预填来源、只读项、切换主体时的显隐规则 |
| **校验** | 必填、格式、长度、材料齐全等与 PRD「点击校验」一致 |
| **主操作落点** | 提交/通过/驳回/保存等按钮文案与位置按 PRD；禁止用「以后再做」暗示已可写 |
| **附件/材料** | 须可上传或可预览（详情只读预览）；禁止仅「已上传」无文件 |
| **子 Tab / 分区** | PRD 规定的 Tab（如详情六 Tab、审核三子 Tab）须有入口；未实现的 Tab 须标明缺口，不得假装已齐 |

**最小壳**：须在回复中逐条列出相对 PRD **缺哪些字段/Tab**；不得让验收人以为详情/表单规格已做完。

## 1. 线框只管业务与流程 · UI 必须跟现网（强制分轨）

| 轨 | 权威 | 要对齐什么 | 禁止什么 |
|----|------|------------|----------|
| **业务 / 流程 / 栏位规格轨** | PRD HTML 线框与规格表 | 栏位与必填、**展示名原文**、**默认列**、**详情/表单/审批字段**、筛选、校验、权限、状态机、按钮语义 | 擅自砍列/砍字段/砍筛选/砍 Tab；擅自改写 PRD 栏位名 |
| **UI / 视觉轨** | **现网已上线产品** | 布局壳、导航、Element Plus、主色、列表/详情密度 | 把线框灰底、便签块原样搬进产品页 |

1. **线框不是视觉稿**：版式/灰底不作为视觉验收；但 **列表列与表单/详情/审批栏位是业务规格，必须齐**。  
2. **「对照线框」** = 栏位·校验·流程·状态·列表/表单完备性；不得像素复刻线框皮。  
3. **禁止展示给用户**：`contact_*` / Phase / P02-A 等内部代号。  
4. **仅当「走不通」才允许偏离**：须登记原因 + 锚点 + 回补。  
5. **最小壳**：须写清相对 PRD 缺的列/字段/筛选/Tab；不得让验收人以为规格已做完。

## 2. 已开发多少 · 测试完成多少（强制 · 禁止偷懒）

**硬规则：功能已合入/已可点 = 对应用例必须测完；冒烟不能顶全量。**

1. **范围对齐**：本批落地的 API/页面/写操作，必须完成 QA 对应用例（R1 + 有页则联测 FE-* / 端页 + 新页则 26）。未实现才允许 Blocked/skip 并写明原因。  
2. **禁止用薄冒烟冒充完成**：仅「路由存在 / 页面能打开 / API 200」不算测完；写操作按联测金标准。  
3. **禁止欠账借口**：不得以「整包以后再写」「先提验再补测」跳过本批已交付范围。  
4. **脚本跟文档 ID**：断言 ID 与 QA 库一致。  
5. **实现一块、启用一块**：去掉本批相关 skip，补真断言。  
6. **新页/大改页**：联测与 UX 走查并列；VIS 跟现网。列表核 §0b；详情/新增/编辑/审批核 §0c。

## 3. 提请用户验收前：AI 必须先自测一遍

| 项 | 要求 |
|----|------|
| 业务对照 | PRD 锚点与业务/流程/**栏位规格**差异（无则写「无偏离」）；有缺口须列字段名 |
| UI 对照 | 是否跟现网壳与同类页一致 |
| 用例对照 | 已跑用例 ID 与结果 |
| 主路径 | 登录 → 目标页 → 关键操作 → 可见结果 |
| 脚本 | 相关 verify / Playwright 结果 |
| 已知缺口 | 相对 PRD 未做的列/字段/Tab、Blocked |

**未自测通过或本批用例未测完 → 禁止提请验收 / 宣称开发完成。**

## 4. 资质材料 / 文件上传（强制 · 全站同规）

1. **真实选文件**；未选中不得「已上传」。  
2. **禁止假上传**。  
3. **先传后识**（须有 `file_id`）。  
4. **必传 / 选填标明**；提交前校验必传。  
5. **复用** `ShopMaterialUpload` 等真实上传组件。  
6. 审核/详情须可 **预览** 附件。

## 5. 反例（禁止）

- 列表砍默认列；详情/审批砍 PRD 栏位；表单缺必填/显隐；**擅自改写 PRD 字段展示名**  
- 未读对应 PRD 锚点就开发/提验  
- 功能可点却只跑冒烟仍报完成  
- 把线框灰底当产品 UI；假上传；内部代号进用户文案  
- 用「占位 Tab」却不声明相对 PRD 缺哪些字段  
