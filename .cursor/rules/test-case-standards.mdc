---
description: 测试用例格式/颗粒度（六段式 + 前后端联测 + UI/UX 专家走查），禁止大纲与纯 API 顶替验收
globs: docs/05-测试与验收/**/*
alwaysApply: false
---

# 测试用例书写标准（强制）

权威全文：

- [01-六段式书写规范](docs/05-测试与验收/测试用例/内容获客商城-phase1/01-六段式书写规范.md)
- [02-前后端联测金标准](docs/05-测试与验收/测试用例/内容获客商城-phase1/02-前后端联测金标准.md)
- [26-UX专家走查](docs/05-测试与验收/测试用例/内容获客商城-phase1/26-UX专家走查.md)
- 包索引：[内容获客商城-phase1/README.md](docs/05-测试与验收/测试用例/内容获客商城-phase1/README.md)

后续**本仓库任意模块**新增/改写测试用例，格式、颗粒度、验收范围均按此标准（不限于商城 phase1）。

## 必须遵守

1. **六段式**：前置 · API/页面（联测须写「将触发的 API」）· 步骤 · 期望 · 边界值表 · 业务场景表 · 结果☐  
2. **写操作 = 前后端联测**：页面填写/点击 → Network 正确请求与状态码 → UI toast/列表/状态；禁止只 Postman/只看文案就勾 Pass  
3. **颗粒度**：禁止「L01～L07 全过」「字段见某某」「应失败」；边界表要有具体输入值与期望 Status/UI  
4. **分层**：R1 纯 API 仅合同/边界；端验收 = 联测（FE-* / 端页）+ **UI/UX 专家走查**；任一缺失不算完成  
5. **UI/UX 专家检查（强制纳入验收）**：新页面/大改版首次可点及版本冻结前，必须按 [26](docs/05-测试与验收/测试用例/内容获客商城-phase1/26-UX专家走查.md) 对 CONS / UX / VIS 打分；单页均分 &lt; 1.5 或不通过；输出 `页面 | CONS | UX | VIS | 阻断项 | 截图 | 结论`  
6. **未实现**：标 Blocked + 阻塞批次；禁止假绿  
7. **分册**：过大则切开；禁止巨石，也禁止冒烟包顶替全页  
8. **已开发多少 · 测试完成多少**：功能已合入/页面已可点 → 必须完成对应用例；禁止「先交付、以后再测」；冒烟门禁不能替代 FE-* / 端页写操作  
9. **栏位完备**：列表默认列、详情/新增/编辑/审批页字段须对照 PRD，不得擅自砍字段；**展示名须与 PRD 原文一致**（见 `.cursor/rules/prototype-acceptance-discipline.mdc` §0b / §0c）

## 验收完成定义（缺一不可）

```text
功能完成 = R1 相关边界（本批 API 脚本）
         + 前后端联测 Pass（02 金标准 / 端页写操作）
         + UI/UX 专家走查 Pass（26：CONS+UX+VIS）
         + 开箱相关项（若在交付范围）
         + 交付范围与已测用例 ID 一一对应（无欠测）
```

仅 API 绿、或仅「页能打开」、或仅联测绿但无 UX 走查 → **不得**宣称页面/版本可交付。

提请用户验收前另须遵守 **`.cursor/rules/prototype-acceptance-discipline.mdc`**：**线框只管业务与流程，UI 跟现网**；**已开发多少测完多少**；**Agent 先自测通过再提验**。

## 联测期望写法（摘要）

```markdown
**页面**: `/shop/...`
**将触发的 API**: `POST /shop/...`

**测试步骤（前端）**:
1. 打开页 → 输入具体值 → 点提交 → 看 Network

**期望结果（双端）**:
- Network: 201/200；关键字段…
- UI: toast/列表…
```

## UI/UX 走查摘要（每页）

| 组 | ID 前缀 | 要点 |
|----|---------|------|
| 一致性 CONS | TC-U-CONS-* | Web 主色 `#1677ff`、顶栏/侧栏、列表心智、空态/Toast；买家顶栏 `#0A1628` |
| 可用性 UX | TC-U-UX-* | 主 CTA、返回、表单错误锚点、防连点、状态色、触控、切店 |
| 视觉 VIS | TC-U-VIS-* | 一屏一任务、栅格、对比度、无紫光/emoji 堆；**跟现网同类页**，不以 PRD 线框视觉为对齐标准 |

端页详表可挂 `TC-U-{页}-01` 截图项，但**不能替代** 26 的全维度打分。

## 反例（禁止交付）

- 端页只有 JSON `POST`、无前端步骤  
- 端页只有「点击成功」、无 Network 断言  
- 「五件套全过」一笔带过  
- 步骤为 `1. —` 或期望仅「应失败」  
- 功能联测绿但跳过 UI/UX 专家走查就宣称可发布  
- 以「和线框长得一样」作为 VIS 通过标准（线框只管业务与流程）  
