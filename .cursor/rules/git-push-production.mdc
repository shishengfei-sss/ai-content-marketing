---
description: 用户说「推送 git」= 推送到生产发布远程，供运维从 master 拉取/发布
alwaysApply: true
---

# 「推送 git」= 生产发布推送

当用户说 **推送 git**、**推 git**、**提交并推送**（且语境为发版/上线）时，默认目标是让 **运维能从 Git 拉取最新 `master` 并发布到生产环境**（`192.168.20.200:8088` / `8089`），而不是仅本地 commit。

## 标准流程（按顺序）

1. **确认工作区已 commit**（用户若只说推送、未 commit，先 `git status`，必要时先提交）
2. **推送到生产 Git 远程**（首选）：
   ```bash
   git push deploy master
   ```
   - 远程：`deploy` → `root@192.168.20.200:/opt/git/ai-content-marketing.git`
   - `post-receive` 钩子会自动：checkout → 同步 API → `alembic upgrade head` → 服务器 `npm run build`（Web/H5）→ 重启 API → reload Nginx
3. **同步 GitHub 备份**（可选但建议）：
   ```bash
   git push origin master
   ```

## 与 `deploy-prod.ps1` 的区别

- **`git push deploy master`**：运维从 Git 拉取的标准路径（用户说的「推送 git」指这个）
- **`scripts/deploy-prod.ps1`**：本地构建后 scp 上传，仅当 deploy 远程不可用或用户明确要求时用

## 推送失败时

- **`deploy` Permission denied**：本机公钥需加入 `192.168.20.200` 的 `root` `authorized_keys`（见 `scripts/setup-deploy-server.sh`）
- **`origin` 超时/认证失败**：提示用户在本机终端重试或配置 GitHub SSH/Token
- **推送成功但页面仍旧**：提醒运维/用户强刷浏览器；确认钩子已跑完 build（仅 `git pull` 源码不够，必须 build dist）

## 数据安全（与运维约定一致）

- 只 `alembic upgrade head`，禁止 downgrade / 删库
- 永不覆盖线上 `.env`、`storage/`、`*.db`
