# QC-SHOP-P1-L4-20260814

| 字段 | 内容 |
|------|------|
| 编号 | QC-SHOP-P1-L4-20260814 |
| 层级 | L4 **研发自检草案**（非研发负责人签字稿） |
| 范围 | 内容获客商城 Phase1：`app/services/shop` · `app/routers/shop` · `app/routers/platform_shop` · `app/routers/mp` 商城 · `apps/web/src/views/shop` · `apps/web/src/views/admin/shop` · Alembic 098+ · 买家 H5 `apps/mp` 店首页/商详 |
| 日期 | 2026-08-14 |
| Commit | `50ef06f` + 本机未提交（H5 排序/试看、测试夹具、本 QC 文档） |
| Alembic | **137**（`apps/api/tests/alembic_head.py`） |
| Mock | 微信 / 抖店 / 短信 / OCR 均为 Mock 档 |
| 扫描依据 | [代码 QC 计划](../../02-执行计划/内容获客平台-代码QC计划.md) §12 · [深度检查手册](../../02-执行计划/内容获客平台-代码QC计划-深度检查手册.md) §2 / §4 / §5 / §7 |
| 配套 QA | [内容获客商城-phase1-测试报告-2026-08-14.md](./内容获客商城-phase1-测试报告-2026-08-14.md) |
| TL / 研发负责人 | **未签** |
| 起草 | 研发自检（本文件） |

> **本文件不是「准许发版」。** 开箱门槛 Q2 要求「代码 QC L4 准许发版/准许交付」须 **研发负责人签字**。本草案结论最多为「有条件准许 Mock 档交付自检」。**不得宣称开箱完成。**

---

## 1. DR-1～DR-10

| ID | 结论 | 证据 / 备注 |
|----|------|-------------|
| DR-1 写接口权限 | **Pass（A20 豁免已代码化）** | 商家域写路由均 `require_permission`。A20 写接口 `Depends(require_self_onboarding)`：登录 + **企业管理员或店铺管理员**（未入驻无 `shop.*`，不能用 `shop.onboarding.*`）。店员 OCR 403。GET status 仍任意成员可读。记 **DEBT-SHOP-004 豁免落地**。 |
| DR-2 租户隔离 | **Pass** | 商家查询走 `ctx.tenant_id` / `_get_owned`。入驻 OCR 按租户目录校验 `file_id`（`assert_onboarding_file_owned`）；跨租户 404（SEC-OCR）。 |
| DR-3 支付+权益同事务/幂等 | **Pass（Mock）** | `order_service.apply_payment_notify`：已 paid + 同 `wx_transaction_id` 短路 `idempotent`；异 txn 拒绝；他单占用同一 txn 有查询。R6 `verify_shop_security_payment.py`；R4 F3/F4。 |
| DR-4 商品状态机 / 挂载闸 | **Pass** | `transition_product(action)` 白名单；`publish` 仅 `approved`/`off_sale`。禁止 `draft→on_sale`、`rejected→on_sale`（VS-M4-SM）。公域映射前校验在售 + 挂载闸（R4 F12 / R5）。 |
| DR-5 买家不成 Contact | **Pass** | `app/services/shop`、`app/routers/mp` **零** `Contact` / `contacts` 引用。 |
| DR-6 PII 脱敏 | **Pass（Mock）** | 买家/订单/支付密钥默认 `*_masked` + reveal。商家列表不含明文。入驻申请 Out / status / 材料 OCR 快照默认脱敏证号与手机；明文仅 reveal。OCR **填表当时**仍回识别明文以便自动填入。 |
| DR-7 Alembic 只 upgrade | **Pass** | 098+ 的 `DELETE FROM` 仅清 `tenant_role_permissions` / 系统角色码，**无**删业务表（订单/商品/商家）。未改 `.env`、未动 `storage/`。 |
| DR-8 Provider | **Pass（Mock）** | 支付走 `wechat_pay_service` mock/live 分支；抖店/短信有独立 mock verify。业务路由不散落 SDK。真机未纳入本 QC。 |
| DR-9 自测脚本 | **Pass（已开发范围）** | R1 896/896 · R4 45/45 · R5 18/18 · R6 32/32 · R7 `run_m0_m8` 经 Agent 套件全过 · CRM `--through 2f` 全过。见 QA 报告 §10。 |
| DR-10 Windows 硬重启 | **Pass（本机既有）** | 提测轮次使用 `scripts/restart-api.ps1`；`GET /health` 端口 **8003**。本批改过入驻闸门 / 领权短信后已硬重启。 |

**DR 总判**：无资金/租户串数据类 P0 拒收项。DR-1 A20 豁免已代码化（企业管理员，非任意成员）。L4 仍 **不得勾「准许发版」**（缺负责人签字 + 产品 SMOKE）。

---

## 2. §2 扫描命中

| 命令（深度手册 §2） | 命中摘要 | 结论 |
|---------------------|----------|------|
| `db.query(Shop` / `.get(` | 商家服务按主键取后校验归属（抽检 `product_service._get_owned`、订单 notify） | Safe |
| 写路由 vs `require_permission` | 除 A20 外均有权限依赖；A20 写走 `require_self_onboarding`（企业管理员） | Safe（DEBT-SHOP-004 豁免落地） |
| OCR file_id | 商家用 JWT 租户目录；平台必填 `tenant_id` 后校验 | Safe（DEBT-SHOP-005 已关） |
| `float(` 金额 | `app/services/shop` **无** `float(` | Safe |
| PII 日志 `logger.*mobile\|id_no` | shop services **无**该类命中 | Safe |
| `Contact\|contacts` shop/mp | **无**命中 | Safe |
| 前端 `v-html` shop/admin shop 视图 | **无**命中 | Safe |
| `#7c3aed` / `purple` | `OrderDetail.vue` 支付渠道曾用 `#722ed1`（非整套紫主题） | 本轮改为现网字色；见债关闭 |
| Alembic 098+ `DELETE FROM` | 仅权限/系统角色，无业务表 | Safe |
| `on_sale` 赋值 | `publish_product` 有前置状态校验 | Safe |

---

## 3. §3 模块走查（抽检）

| # | 路径 | 问答 | 结论 |
|---|------|------|------|
| 1 | `permissions.py` | 商城权限码与内置角色已落地；店员三权未在本轮重数 37/18 | Yes（未复盘数量差） |
| 2 | `models/shop.py` | 未逐字段对 04 数据模型全文 | 部分（未做字段级对表） |
| 3 | `schemas/shop_platform.py` | 入驻 Out 默认脱敏；列表已滤；明文仅 reveal | Yes |
| 4 | `onboarding_*.py` | 重复 pending→409 有服务层；OCR stub 不自动过审 | Yes |
| 5 | `merchant_service.py` | 列表 SQL 层 scope（R1/R2 覆盖） | Yes |
| 6 | `service_log_service.py` | 仅追加 | Yes（抽检） |
| 7 | `routers/platform_shop` | 写接口有 platform 权限码 | Yes |
| 8 | `routers/shop/onboarding.py` | 鉴权仅租户登录；OCR 未绑 file 归属 | No → 债 |
| 9 | `auth_workspace` | 切工作区换发权限（既有登录流） | Yes（未本轮重读） |
| 10 | alembic 098～103+ | upgrade 至 137；P05 人工档已由 `verify_shop_p05.py` 覆盖 | DEBT-SHOP-001 **已关闭**（2026-08-15） |
| 11 | `verify_shop_m0.py` | 30/30 | Yes |
| 12 | Login/Register | 「注册≠入驻」文案既有 | Yes（未本轮重走 UI） |

手册「已观察味道」核实：

| 味道 | 本轮 |
|------|------|
| OCR 无细权限 | **仍在** → DEBT-SHOP-004 |
| OCR `_ = ctx` | **已修**：`assert_onboarding_file_owned(ctx.tenant_id, file_id)`；平台 OCR 须带 `tenant_id` |
| 102 无 API | **已关闭** → DEBT-SHOP-001（P05 人工档，不对接银行） |

---

## 4. 高风险剧本

### 4.1 支付 / 权益（手册 §4）

| 问 | 答 |
|----|----|
| 验签 | Mock 档 `sign` 由 `apply_payment_notify` 处理；真机证书不在本 QC |
| 找单 | `order_no`；找不到不枚举业务成功（脚本覆盖） |
| 金额 | 与 `paid_amount_cents` 比较 |
| 已 paid | 同 txn 幂等返回 |
| 锁 | 同 txn 短路 + 他单占用检查；无分布式锁 |
| 权益 | 支付成功路径开通；R4 F3/F5；退款后不可履约 R4 F6 |
| 事务 | 单次 commit 路径；R6 支付篇 27 条中含 notify 幂等 |
| 测试 | `verify_shop_security_payment.py` · `verify_shop_e2e_f3.py` |

**Pass（Mock）。** 真机验签/证书 **未做**。

### 4.2 商品状态机（手册 §5）

允许边由 `transition_product`（`PRODUCT_TRANSITION_ACTIONS`）执行。抽检无 `draft→on_sale` API。**Pass。**

### 4.3 L4 安全表（手册 §7）

| ID | 结果 | 方法 |
|----|------|------|
| SEC-01 IDOR | Pass（脚本） | R6 permissions |
| SEC-02 水平越权 | Pass（脚本） | 同上 |
| SEC-03 垂直越权 | Pass（脚本） | 商家调 `/admin/shop` |
| SEC-04 Webhook 伪造 | Pass（Mock） | R6 支付 / R5 |
| SEC-05 重放 | Pass（Mock） | notify 幂等 |
| SEC-06 批量 | 未单列 page_size=100000 人工 | 列表 Query `le=100` 抽检专栏/商品 |
| SEC-07 文件 | Pass | 入驻 OCR 校验 file_id 属当前租户目录；非法标识 422；跨租户 404 |
| SEC-08 XSS | 前端 shop 无 `v-html` | 未做 payload 入库打点 |
| SEC-09 敏感日志 | shop 无 mobile 日志命中 | 未打开生产日志目视 |
| SEC-10 依赖 CVE | 未做 | 本轮无新包审查 |

---

## 5. 测试映射（抽 3 条 + 批次出口）

| TC / 脚本 | 结果 |
|-----------|------|
| R6 安全全集 `verify_shop_security_*.py` | 32/32 |
| R4 E2E `verify_shop_e2e_f*.py` | 45/45 |
| R7 `verify_shop_m0.py` + CRM + Agent | M0 30/30；CRM 2f 全过；Agent T0～AG8 全过 |
| R5 Mock | 18/18 |
| BOX-D1/D2/D7 | `verify_shop_box.py` 15/15 研发自检 |

---

## 6. 债务

| ID | 动作 | 说明 | 到期 |
|----|------|------|------|
| DEBT-SHOP-001 | **关闭（范围收口）** | P05 API+UI 已由 `verify_shop_p05.py` 覆盖。打款人工确认、导出 CSV 非 PDF、不对接银行，属 Phase1 范围而非「无 API」 | 2026-08-15 |
| DEBT-SHOP-002 | **关闭** | 商城前端已建（平台/商家 Web + 买家 H5） | — |
| DEBT-SHOP-003 | **关闭** | `run_shop_all` 与 UI 自动化已建 | — |
| DEBT-SHOP-004 | **豁免落地** | 不新增 `shop.onboarding.*`；写接口 `require_self_onboarding`（企业管理员）。店员不可 OCR | 2026-08-14 |
| DEBT-SHOP-005 | **关闭** | OCR 已按租户目录校验 file_id；平台 OCR 必填 tenant_id | 2026-08-14 |
| DEBT-SHOP-007 | **关闭** | 入驻 `_to_out` / status / 材料 OCR 快照默认脱敏；A20 驳回后敏感栏清空重填 | 2026-08-14 |
| DEBT-SHOP-008 | **关闭** | 商品状态边收敛为 `PRODUCT_TRANSITION_ACTIONS` + `transition_product` | 2026-08-14 |

资金/跨租户 P0 债：**无。** 001 已关：P05 人工档已交付，禁止宣称对接银行打款。

---

## 7. 结论（勾选）

| 选项 | |
|------|--|
| 准许提测 QA R1 | ☐ 本批 QA 已跑完，非本结论对象 |
| **准许发版** | ☐ **不勾** |
| **准许交付（开箱 Q2）** | ☐ **不勾**（缺负责人签字；DR 有条件项） |
| **有条件准许 Mock 档交付自检** | ☑ 自动化绿 + 无 P0 资金/串租户；债已显式登记 |
| 驳回 | ☐ 无 P0 拒收，但亦非无条件通过 |

**阻塞开箱宣称的项（研发不得代勾）：**

1. 研发负责人未在本页签字  
2. 产品本人未勾 BOX-SMOKE / D3 / D5  
3. Q2 字面「准许发版/准许交付」未满足  

**签字**

| 角色 | 姓名 | 日期 | 意见 |
|------|------|------|------|
| 起草（研发自检） | — | 2026-08-14 | 有条件准许 Mock 档自检；**不准许发版** |
| Tech Lead | | | |
| 研发负责人 | | | **必签后 Q2 方可讨论** |
