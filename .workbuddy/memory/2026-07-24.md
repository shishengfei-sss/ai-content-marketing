# 2026-07-24 工作日志

## 新增《AI资深服务经理-总体方案.md》（补齐三中枢第三块）
- 用户明确三块为「营销AI、销售AI、服务AI」，要求把服务AI补上，凑齐客户全生命周期三中枢。
- 文件：E:\ai-content-marketing\AI资深服务经理-总体方案.md（v1.0-plan，结构与前两份完全一致：〇现状关系→①职责全景→②形态场景→③架构→④数据地基→⑤原则→⑥路线图→⑦MVP→⑧创业视角→⑨风险拍板→⑩下一步）。
- 核心增量=**三中枢协同架构**：营销(MKT)→销售(SAL)→服务(SVC)三并列 Supervisor 共享底座，经三条 SLA 协同线闭环：成交移交SLA(销售→服务)、增购回流SLA(服务→销售)、口碑/转介绍SLA(服务→营销)。
- 服务经理 10 职责域、8 子Agent(工单官/支持官/成功官/知识官/体验官/内容官/教练官/协同官)；数据地基=CRM(已有)+5新建库(工单服务库/知识库KB/客户健康度库/服务体验库/解决方案案例库)。
- MVP 第一刀建议=Phase1 人格+Supervisor + Phase2 支持官/工单官/体验官，仅用 CRM+轻量工单库+初版KB。
- 末尾提议：把现有 ai-dual-manager-plan 升级为 ai-triple-manager-plan（双中枢→三中枢统一架构图），待用户确认是否做。

## 把服务AI正式整合进统一方案 HTML（ai-dual-manager-plan）
- 用户指 `e:\ai-content-marketing\ai-dual-manager-plan\ai-dual-manager-plan.html` 作参考。经核对：正文已含服务（第3章B摘要、十二（续）服务资料库、第19章修正图），但**图表源文件 `assets/charts.js` 仍是双中枢版**，且正文 3 处漏服务。
- 已改 `assets/charts.js`（4 张图全补服务，JS 语法 `node --check` 通过）：
  - 图1 架构树：加「AI服务经理Supervisor·P-SVC·3专家Agent」节点 + 数据层加「服务六库」，根名改三中枢。
  - 图2 雷达：加「AI服务经理（6大职责域）」第三系列（映射到10轴打分 [5,3,5,10,8,0,7,3,2,8]）。
  - 图3 数据资产：加 L4 服务专属数据（服务六库），原共享降为 L5，L1/L2/L3 标签改三中枢共用。
  - 图4 甘特：加 P2-SVC/P3-SVC 阶段，P1 改三人格、P4/P5 改三Manager。
- 已改 HTML 正文 3 处：① H1 标题加「+ AI资深服务经理」；② 第4章对比矩阵由 营销/销售两列+交叉点 扩为 四列（加 AI服务经理列 + 三向边界/协同）；③ 第14章补 14.7 ServiceManagerSupervisor（P-SVC人格/技能/工作流/提示词）+ 14.8 服务子Agent规格表（服务官/客户健康官/知识官）。
- 采用 HTML 既有「3 服务子Agent」模型（服务官/客户健康官/知识官），与 HTML 全文（第3章B/十二续/第19章/执行摘要统计）一致；**与早前独立写的 `AI资深服务经理-总体方案.md` 的 8 子Agent 模型不一致**——两份文件需二选一或对齐。
- 遗留：文件名仍叫 ai-dual-manager-plan（内容已三中枢），建议改名 ai-triple-manager-plan.html；图表需浏览器打开本地文件才能渲染（依赖相对路径 echarts.min.js）。

## 给统一方案 HTML 增加侧边导航
- 用户要求 `ai-dual-manager-plan.html`「增加侧边导航」。已加：左侧固定 `.sidebar`（272px）+ 顶部汉堡 `.nav-toggle` + 遮罩 `.sidebar-backdrop`。
- 桌面端(>1100px)：侧栏常驻，正文 `body{padding-left:292px}` 右移，并隐藏原顶部双栏 `.toc`（由侧栏取代）；移动端(≤1100px)：侧栏收成抽屉，点汉堡滑出、点遮罩/链接关闭。
- 侧栏 23 条锚点逐项镜像原 TOC（sec-exec…sec19），脚本 scroll-spy 按 `getBoundingClientRect().top+scrollY` 高亮当前章并把高亮项自动滚入侧栏可视区；全部 23 个 id 已核验存在。

## 用户确认「1+2 都要」：重命名 + 统一服务子Agent口径
- ① 重命名：`ai-dual-manager-plan/ai-dual-manager-plan.html` → `ai-triple-manager-plan.html`（文件夹名未改；相对引用 assets/_shared 不受影响，安全）。workspace 仅记忆文件与本 .md 引用旧名，无外部死链。
- ② 统一口径：以 HTML 的 **3 服务子Agent（服务官/客户健康官/知识官）** 为 canonical，把 `AI资深服务经理-总体方案.md` 从原 8 Agent（工单官/支持官/成功官/知识官/体验官/内容官/教练官/协同官）收拢对齐。
  - .md 改动点：摘要「8→3」；〇表「内容官Agent→服务官内容产出」「成功官/工单官→服务官/客户健康官」「体验官数据底→服务官」；一章职责映射表 10 域全部改挂 服务官/客户健康官/知识官；三章 mermaid 子Agent A1-A8→B1-B3(3个)；六章 Phase2「支持官+工单官+体验官」→「服务官+客户健康官+知识官」，Phase3「完整职能子Agent」→「三大子Agent深化+覆盖10域」；七章 MVP demo 改 3 Agent 表述；九章待拍板 MVP 范围改 3 Agent；十章下一步「支持官」→「服务官」；文末引用旧文件名/双中枢→三中枢。
  - 终检：.md 无旧 Agent 名残留（服务官×21/客户健康官×11/知识官×8）；HTML 服务层已为 3 Agent，销售层保留其 8 Agent（内容官/教练官/协同官等属销售，非冲突）。
- 给用户留回退口：若更想要 .md 的 8-Agent 细分解，可反向把 HTML 改过去（会动 HTML 统计/图表/14.7-14.8，工作量更大）。

## 通读核对 `ai-triple-manager-plan.html` 全文并修复 8 处
- 逐章精读（执行摘要/1-3b/4/6/12b/14/15/16/17/18/19）+ 交叉验证 `assets/charts.js` + 锚点/结构。
- 已确认自洽项：14子Agent(营销3+销售8+服务3)、6+3+6库、10/7/6职责域、雷达10轴、3条SLA协同、服务3Agent(服务官/客户健康官/知识官)全篇统一。
- 修复的不一致：
  1. 章节编号「十七」重复（风险与假设与依赖都标十七）→ 顺延为 十八/十九/二十。
  2. 第6章 L3 销售专属数据多列「招标线索」（执行摘要/图表均说销售3库）→ 删，L3=销售线索库/产品价库/CPQ引擎。
  3. 14.8 知识官标 Phase3，与甘特 P2-SVC「服务三子Agent」+MVP矛盾 → 改 Phase2。
  4. 第1章「服务知识官」措辞 → 统一「知识官」。
  5. charts.js 架构树销售只画4子Agent（标签写8）→ 补齐 线索官/客户官/教练官/协同官(共8)。
  6. 路线图正文 Phase1/2/3 仍是「两个人格/营销+销售」旧双中枢文案，与甘特(三人格/P2-SVC/P3-SVC)不符 → 同步为三中枢（Phase1三人格三Supervisor；Phase2/3加服务侧）。
  7. 路线图开头「销售经理和营销经理并行」→ 改「三Manager」。
  8. 第16章 MVP 卡片「两个人格+两个Supervisor」点明是「MVP首刀范围」，避免与已改三人格的 Phase1 混淆。
- 结构校验：<section> 23开23闭（grep 多算的1个是 scroll-spy 脚本 `sections.length` 误匹配），<main> 正常；charts.js `node --check` 通过。
- 遗留（跨文档，未动）：独立 .md 说 服务5新建库/10职责域，HTML 说 服务6库/6职责域；Agent模型已对齐(3)，库数/职责域数两份仍不一致，待用户决定是否对齐。另 charts.js 数据层 L1 CRM 仅画5实体(正文L1列8)、销售节点合并写法，属可视化简化。

## 第二轮深度核对 `ai-triple-manager-plan.html`（17:03 起）
- 用户再次要求「核对里面的内容有没有问题」。本轮补验上一轮未细核处：工具数16/16/12 是否真列满、服务6库具体名称四处一致性、共享组件表列、图表标签。
- 新发现并修复 5 处：
  1. **第5章「共享组件清单」表漏服务列**（同章卡片已加服务，明细表仍是营销/销售双列）→ 加「服务Manager用法」列（9 行各补服务用法：ServiceManagerSupervisor封装/服务六库检索/P-SVC人格/读写Ticket等/知识官取FAQ素材…）。
  2. charts.js L3 销售标签「营销/服务只读」→「营销/服务不访问」（服务不碰销售库，与权限矩阵一致）。
  3. charts.js L4 服务标签「营销/服务只读(FAQ)」→「营销/销售只读(FAQ)」（服务是属主应读写，笔误把销售写成服务）。
  4. charts.js L1 CRM 仅 5 实体 → 补 Product产品/Ticket工单/Satisfaction满意度（正文 L1 列 8 个，现图表与正文对齐）。
  5. 层级编号：正文用 L3B/L4，图表用 L4/L5 → 正文 L3B:服务专属→L4、L4:共享→L5，与图表对齐。
- 全部 `node --check` 通过；23 锚点/结构仍完好。
- **软项未擅改（待用户定）**：执行摘要/第5章卡说「营销16+销售16+服务12 工具」，但营销子Agent明细实际列到 Strategist8+Creator8(4新+4复用)+Campaigner4=**20 个**，与 16 口径对不上；销售/服务 12 在 HTML 仅摘要级（明细在各自 .md）。建议用户复核工具计数口径或把摘要改 20。
- 双中枢残留兜底扫描：无冲突残留；剩余「营销+销售」均系有意表述（MVP首刀范围/套餐定价/全生命周期闭环原句含三者）。
